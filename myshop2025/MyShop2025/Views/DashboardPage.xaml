<Page
    x:Class="MyShop2025.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="16">
        <StackPanel Spacing="16">
            <TextBlock Text="Bảng điều khiển" FontSize="28" FontWeight="Bold"/>

            <Grid ColumnSpacing="12" RowSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefault}"
                        Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefault}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Tổng sản phẩm" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}"/>
                        <TextBlock x:Name="TotalProductsText" FontSize="28" FontWeight="Bold" Text="0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefault}"
                        Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefault}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Đơn hôm nay" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}"/>
                        <TextBlock x:Name="TotalOrdersText" FontSize="28" FontWeight="Bold" Text="0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefault}"
                        Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefault}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Doanh thu hôm nay" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}"/>
                        <TextBlock x:Name="TotalRevenueText" FontSize="28" FontWeight="Bold" Text="0 ₫"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="3" CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefault}"
                        Padding="16" BorderBrush="{ThemeResource CardStrokeColorDefault}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Lợi nhuận hôm nay" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}"/>
                        <TextBlock x:Name="TotalProfitText" FontSize="28" FontWeight="Bold" Text="0 ₫"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="8">
                    <TextBlock Text="Sắp hết hàng" FontSize="18" FontWeight="SemiBold"/>
                    <ListView x:Name="LowStockList" DisplayMemberPath="Name" MaxHeight="200"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="8">
                    <TextBlock Text="Bán chạy nhất" FontSize="18" FontWeight="SemiBold"/>
                    <ListView x:Name="TopSellingList" MaxHeight="200">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                    <TextBlock Text="{Binding TotalSold}" Foreground="{ThemeResource TextFillColorSecondary}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Grid>

            <StackPanel Spacing="8">
                <TextBlock Text="3 Đơn hàng gần nhất" FontSize="18" FontWeight="SemiBold"/>
                <ListView x:Name="RecentOrdersList" MaxHeight="200">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding OrderId}" FontWeight="Bold"/>
                                <TextBlock Grid.Column="1" Text="{Binding CreatedTime}"/>
                                <TextBlock Grid.Column="2" Text="{Binding FinalPrice}" Foreground="{ThemeResource SystemAccentColor}"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>

            <StackPanel Spacing="8">
                <TextBlock Text="Doanh thu theo ngày (tháng này)" FontSize="18" FontWeight="SemiBold"/>
                <Border CornerRadius="8" Background="{ThemeResource CardBackgroundFillColorDefault}"
                        Padding="16" Height="300">
                    <canvas:CanvasControl x:Name="RevenueChart" Draw="RevenueChart_Draw" Height="260"/>
                </Border>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
