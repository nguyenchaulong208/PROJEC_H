<Page
    x:Class="MyShop2025.Views.ProductsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="12">
            <TextBlock Text="Quản lý sản phẩm" FontSize="28" FontWeight="Bold"/>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="CategoryCombo" Grid.Column="0" Header="Loại sản phẩm"
                          SelectionChanged="CategoryCombo_SelectionChanged">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding name}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <TextBox x:Name="SearchBox" Grid.Column="1" Header="Tìm kiếm" PlaceholderText="Nhập tên hoặc SKU..."
                         TextChanged="SearchBox_TextChanged"/>

                <TextBox x:Name="MinPriceBox" Grid.Column="2" Header="Giá từ" PlaceholderText="0"/>
                <TextBox x:Name="MaxPriceBox" Grid.Column="3" Header="Đến" PlaceholderText="999999999"/>
                <Button Grid.Column="4" Content="Lọc" Margin="0,24,0,0"
                        Click="FilterButton_Click" Height="36"/>
            </Grid>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="+ Thêm sản phẩm" Click="AddProductButton_Click"/>
                <Button Grid.Column="1" Content="+ Thêm loại" Click="AddCategoryButton_Click"/>
                <Button Grid.Column="2" Content="Import Excel" Click="ImportButton_Click"/>
                <TextBlock Grid.Column="4" x:Name="TotalText" VerticalAlignment="Center"
                           Foreground="{ThemeResource TextFillColorSecondary}"/>
            </Grid>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListView x:Name="ProductList" Grid.Row="0" Margin="0,8,0,0"
                      SelectionChanged="ProductList_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnSpacing="12" Padding="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="80"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding sku}" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding name}"/>
                            <TextBlock Grid.Column="2" Text="{Binding categoryName}"/>
                            <TextBlock Grid.Column="3" Text="{Binding count}"/>
                            <TextBlock Grid.Column="4" Text="{Binding importPrice}"/>
                            <TextBlock Grid.Column="5" Text="{Binding salePrice}"/>
                            <StackPanel Grid.Column="6" Orientation="Horizontal" Spacing="4">
                                <Button Content="Sửa" Click="EditProductButton_Click"/>
                                <Button Content="Xóa" Click="DeleteProductButton_Click"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8" Margin="0,8">
                <Button x:Name="PrevButton" Content="Trước" Click="PrevButton_Click"/>
                <TextBlock x:Name="PageText" VerticalAlignment="Center"/>
                <Button x:Name="NextButton" Content="Sau" Click="NextButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
